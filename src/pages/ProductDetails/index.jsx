import React from 'react'

const ProductDetails = (props) => {
    let { id } = props;
    return (
        <div>
            This is ProductDetails with id {id} page
        </div>
    )
}

export default ProductDetails
